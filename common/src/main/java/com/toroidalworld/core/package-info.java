/**
 * The vocabulary every other group names — the fold contract and its factory, the world bounds, the option framework,
 * the registry boundary — reading nothing of the mod but the ducks of {@code accessors} and the loader seam of
 * {@code platform}.
 */
package com.toroidalworld.core;
