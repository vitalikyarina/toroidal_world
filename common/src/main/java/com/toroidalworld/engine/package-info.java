/**
 * The work behind the fold — the folds themselves, the periodic noise, generation, packet translation and the seam
 * halves of vanilla behaviour — read by the {@code mixin} packages, {@code accessors}, {@code compat},
 * {@code platform}, {@code client.engine} and the loader entries alone.
 */
package com.toroidalworld.engine;
