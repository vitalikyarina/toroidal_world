pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = 'FabricMC'
            url = 'https://maven.fabricmc.net/'
        }
    }

    plugins {
        id 'net.neoforged.moddev' version '2.0.143'
        // Loom ships two plugin ids: 'net.fabricmc.fabric-loom' is LoomNoRemapGradlePlugin, which forces
        // disableObfuscation and suits the unobfuscated 26.x line; 1.21.x is obfuscated and needs the remapping one.
        id 'net.fabricmc.fabric-loom-remap' version "${fabric_loom_version}"
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'toroidal-world'

include 'common'
include 'neoforge'
include 'fabric'
